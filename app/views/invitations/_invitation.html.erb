<%= link_to invitation.user.company_name + "- " + invitation.created_at.to_s, invitation %>
<% if invitation.approval %>
	(you accepted)
<% else %>
	<%= button_to "Accept", :controller => "invitations", :action => "accept", :id => invitation.id %>
<% end %>

<br>